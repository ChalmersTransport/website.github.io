{
  "name": "Hugo Extended",
  "features": {
    "ghcr.io/devcontainers/features/hugo:1": {
      "version": "0.147.8",
      "variant": "hugo_extended"
    }
  },
  "ports": [
    { "port": 1313, "onAutoForward": "openBrowser" }
  ],

  // 
  "containerEnv": { "PATH": "/usr/local/bin:${PATH}" },

  // 
  "postCreateCommand": "set -e; cd /tmp && curl -L -o hugo.tar.gz https://github.com/gohugoio/hugo/releases/download/v0.147.8/hugo_extended_0.147.8_linux-amd64.tar.gz && tar -xzf hugo.tar.gz && sudo install -m 0755 hugo /usr/local/bin/hugo && hugo version"
}
